import React from 'react';
import Thumbnail from '../src/Thumbnail';
import Modal from '../src/Modal';
import DescriptionForm from '../src/DescriptionForm'
import { shallow, mount, render } from 'enzyme';

//TO-DO check children rendering
// describe('<Thumbnail />', () => {
//   it('initial should not render Modal', () => {
//     const wrapper = shallow(
//     <Thumbnail />
//     );
//     expect(wrapper.find(Modal)).to.have.length(0);
//   });
// });

// describe('<Thumbnail />', () => {
//   it('initial should render empty <div>', () => {
//     const wrapper = shallow(
//     <Thumbnail />
//     );
//     wrapper.contains(<div id="nth"></div>).to.equal(true);
//   });
// });

// describe('<Modal />', () => {
//   it('initial should not render DescriptionForm', () => {
//     const wrapper = shallow(
//     <Modal />
//     );
//     expect(wrapper.find(DescriptionForm)).to.have.length(0);
//   });
// });


